```javascript
// MyComponent.jsx
import React from 'react';

export default function MyComponent() {
  return (
    <div className="bg-gray-100 p-4">
      <h1 className="text-3xl font-bold">My Component</h1>
    </div>
  );
}
```
This component uses Tailwind CSS classes for styling, but the class `bg-gray-100` does not work. The background remains white instead of light gray.